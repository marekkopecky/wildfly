<?xml version='1.0' encoding='UTF-8'?>
<!--
  ~ Copyright The WildFly Authors
  ~ SPDX-License-Identifier: Apache-2.0
  -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.jboss.bom.builder</groupId>
        <artifactId>wildfly-server-ee</artifactId>
        <!--
        Maintain separation between the artifact id and the version to help prevent
        merge conflicts between commits changing the GA and those changing the V.
        -->
        <version>8.2.0.Alpha-SNAPSHOT</version>
    </parent>

    <packaging>pom</packaging>

    <artifactId>wildfly-server-ee-standard-builder</artifactId>

    <name>WildFly BOM Builders: Server EE Standard Builder</name>
    <description>This artifact builds a bill of materials (BOM) for WildFly Server EE Standard's Dependency Management</description>

    <dependencyManagement>
        <dependencies>
            <!-- base import of server's dep management -->
            <dependency>
                <groupId>org.jboss.eap</groupId>
                <artifactId>wildfly-standard-ee-bom</artifactId>
                <version>${ee.maven.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.wildfly.plugins</groupId>
                <artifactId>wildfly-bom-builder-plugin</artifactId>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>build-bom</id>
                        <goals>
                            <goal>build-bom</goal>
                        </goals>
                        <configuration>
                            <bomParent>
                                <groupId>org.jboss</groupId>
                                <artifactId>jboss-parent</artifactId>
                                <relativePath/>
                            </bomParent>
                            <bomGroupId>${bomGroupId}</bomGroupId>
                            <bomArtifactId>${bomArtifactIdPrefix}-ee-11</bomArtifactId>
                            <bomVersion>${project.version}</bomVersion>
                            <bomName>${full.dist.product.release.name} BOMs: EE 11</bomName>
                            <bomDescription>${full.dist.product.release.name}: EE 11 Dependency Management</bomDescription>
                            <channels>
                                <channel>
                                    <manifest>
                                        <groupId>org.jboss.eap.channels</groupId>
                                        <artifactId>${latest.ee.channel.artifactId}</artifactId>
                                        <version>${version.boms.channel.ee}</version>
                                    </manifest>
                                </channel>
                            </channels>
                            <includeDependencies combine.children="append">
                                <dependency>
                                    <groupId>jakarta.data</groupId>
                                    <artifactId>jakarta.data-api</artifactId>
                                </dependency>
                                <dependency>
                                    <groupId>org.jboss.spec.jakarta.el</groupId>
                                    <artifactId>jakarta.el-api</artifactId>
                                </dependency>
                                <dependency>
                                    <groupId>jakarta.enterprise</groupId>
                                    <artifactId>jakarta.enterprise.cdi-el-api</artifactId>
                                </dependency>
                                <!-- replaces org.hibernate.orm:hibernate-jpamodelgen -->
                                <dependency>
                                    <groupId>org.hibernate.orm</groupId>
                                    <artifactId>hibernate-processor</artifactId>
                                </dependency>
                            </includeDependencies>
                            <versionRefDependencies combine.children="append">
                                <dependency>
                                    <groupId>org.hibernate.orm</groupId>
                                    <artifactId>hibernate-processor</artifactId>
                                    <version>org.hibernate.orm:hibernate-core:jar</version>
                                </dependency>
                            </versionRefDependencies>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <modules>
        <module>with-tools</module>
    </modules>
</project>
